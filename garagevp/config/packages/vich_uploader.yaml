vich_uploader:
    db_driver: orm
    mappings:
        car_images:                                          
            uri_prefix: /uploads                        # Préfixe URL pour accéder aux images
            upload_destination: '%kernel.project_dir%/public/uploads'    # Dossier de destination
            namer: Vich\UploaderBundle\Naming\PropertyNamer  # Nommage automatique des fichiers

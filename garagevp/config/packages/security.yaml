# security:
#    # Hash passwords
#    password_hashers:
#        App\Entity\User:
#            algorithm: bcrypt
#            cost: 12

#    # User provider
#    providers:
#        app_user_provider:
#            entity:
#                class: App\Entity\User
#                property: email

#    firewalls:
       
#        dev:
#            pattern: ^/(_(profiler|wdt)|css|images|js)/
#            security: false
#        # Main security config
#        main:
#            lazy: true
#            provider: app_user_provider
#            custom_authenticator: App\Security\LoginFormAuthenticator
#            form_login:
#                login_path: app_login
#                check_path: app_login
#                enable_csrf: true
#            entry_point: form_login
#            logout:
#                path: app_logout
#                target: app_home
#            remember_me:
#                secret: '%kernel.secret%'
#                lifetime: 604800 # 1 week
#                secure: true
#                always_remember_me: true
#            login_throttling:
#                max_attempts: 3
#                interval: '15 minutes'

#        # Admin interface
#        easyadmin_admin:
#            pattern: ^/admin
#            security: true

#        # Employee interface
#        easyadmin_employe:
#            pattern: ^/easyemploye/dashboard
#            provider: app_user_provider
#            custom_authenticator: App\Security\LoginFormAuthenticator

#    # Route access control
#    access_control:
#        - { path: ^/easyemploye/dashboard, roles: ROLE_EMPLOYE }
#        - { path: ^/admin, roles: ROLE_ADMIN }
#        - { path: ^/, roles: PUBLIC_ACCESS }
    








security:
    # Encoders
    password_hashers:
        App\Entity\User:
            algorithm: bcrypt
            cost: 12

    # Providers
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    # Firewalls
    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false
            
        login:
            pattern: ^/api/login
            stateless: true
            json_login:
                check_path: /api/login
                success_handler: lexik_jwt_authentication.handler.authentication_success
                failure_handler: lexik_jwt_authentication.handler.authentication_failure

        api:
            pattern: ^/api
            stateless: true
            jwt: ~

        main:
            lazy: true
            provider: app_user_provider
            custom_authenticator: App\Security\LoginFormAuthenticator
            form_login:
                login_path: app_login
                check_path: app_login
                enable_csrf: true
            entry_point: form_login
            logout:
                path: app_logout
                target: app_home
            remember_me:
                secret: '%kernel.secret%'
                lifetime: 604800
                secure: true
                always_remember_me: true
            login_throttling:
                max_attempts: 3
                interval: '15 minutes'

    # Access Control
    access_control:
        - { path: ^/api/login, roles: PUBLIC_ACCESS }
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/easyemploye/dashboard, roles: ROLE_EMPLOYE }
        - { path: ^/admin, roles: ROLE_ADMIN }





# _________________________ configuration server sans le https _______________________________

#  server {
#     listen 80;
#     server_name localhost;
#     root /var/www/html/public;
#     index index.php index.html;

#     # Compression gzip
#     gzip on;
#     gzip_types text/plain text/css application/json application/javascript text/xml application/xml text/javascript;
#     gzip_min_length 1000;
#     gzip_comp_level 6;

#     # Cache des fichiers statiques
#     location ~* \.(jpg|jpeg|gif|png|css|js|ico|xml)$ {
#         access_log off;
#         log_not_found off;
#         expires 30d;
#         add_header Cache-Control "public, no-transform";
#     }

#     location / {
#         try_files $uri /index.php$is_args$args;
#     }

#     location ~ ^/index\.php(/|$) {
#         fastcgi_pass symfony_app:9000;
#         fastcgi_split_path_info ^(.+\.php)(/.*)$;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
        
#         # Buffers pour de meilleures performances
#         fastcgi_buffers 16 16k;
#         fastcgi_buffer_size 32k;
        
#         # Cache FastCGI
#         fastcgi_cache_use_stale error timeout http_500;
#         fastcgi_cache_valid 200 60m;
        
#         internal;
#     }

#     location ~ \.php$ {
#         return 404;
#     }

#     # Logs
#     error_log /var/log/nginx/error.log warn;
#     access_log /var/log/nginx/access.log;
# }








# server {
#     listen 443 ssl;
#     listen [::]:443 ssl;
#     server_name localhost;
#     root /var/www/html/public;
#     index index.php index.html;

#     # Chemins exacts des certificats
#     ssl_certificate /etc/ssl/private/localhost.crt;
#     ssl_certificate_key /etc/ssl/private/localhost.key;
    
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH;
#     ssl_ecdh_curve secp384r1;
#     ssl_session_timeout  10m;
#     ssl_session_cache shared:SSL:10m;
#     ssl_session_tickets off;
#     ssl_stapling on;
#     ssl_stapling_verify on;

#     location / {
#         try_files $uri /index.php$is_args$args;
#     }

#     location ~ ^/index\.php(/|$) {
#         fastcgi_pass symfony_app:9000;  # Utilisez le nom du service Docker
#         fastcgi_split_path_info ^(.+\.php)(/.*)$;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#         fastcgi_param PATH_INFO $fastcgi_path_info;
#         fastcgi_param HTTPS on;
#         internal;
#     }

#     # Redirection HTTP vers HTTPS
#     error_page 497 https://$host:$server_port$request_uri;
# }

# # Redirection HTTP vers HTTPS
# server {
#     listen 80;
#     listen [::]:80;
#     server_name localhost;
#     return 301 https://$host8443$request_uri;
# }



server {
    listen 80;
    listen [::]:80;
    server_name localhost garagevp.local;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name localhost garagevp.local;
    
    root /var/www/html/public;
    index index.php index.html;

    ssl_certificate /etc/ssl/private/localhost.crt;
    ssl_certificate_key /etc/ssl/private/localhost.key;
    
    # Configuration SSL améliorée
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384;
    ssl_prefer_server_ciphers off;
    
    # Configuration des sessions SSL
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:50m;
    ssl_session_tickets off;

    location / {
        try_files $uri /index.php$is_args$args;
    }

    location ~ ^/index\.php(/|$) {
        fastcgi_pass app:9000;
        fastcgi_split_path_info ^(.+\.php)(/.*)$;
        include fastcgi_params;
        
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        fastcgi_param DOCUMENT_ROOT $realpath_root;
        
        fastcgi_buffer_size 128k;
        fastcgi_buffers 4 256k;
        fastcgi_busy_buffers_size 256k;
        
        internal;
    }

    location ~ \.php$ {
        return 404;
    }
}





